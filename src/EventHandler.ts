import GP3Notes from "main";
import { Editor, MarkdownView, Menu } from "obsidian";

export class EventHandler {
	constructor(private plugin: GP3Notes) {}

	// Register events that need to fire when the app starts
	setup() {}
}
